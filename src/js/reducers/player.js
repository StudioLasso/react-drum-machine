const initialState = {
  time: 0,
  elapsedtime: 0,
  measurecopied:[],
  currentbeat: 0,
  currentdivision: 0
};

export default function player(state= initialState, {type, payload} = {}) {
	switch (type) {
	default:
		return state;
	}
}